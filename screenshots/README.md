# Screenshots

Drop your lab screenshots here and reference them from the docs. Suggested set:

- `sconfig-menu.png` — Server Core SConfig menu on first boot
- `forest-promotion.png` — `Install-ADDSForest` completing
- `get-aduser.png` — `Get-ADUser -Filter *` output
- `client-joined.png` — `Get-ComputerInfo` showing `corp.lab.local`
